mixin fieldName(parent)
	div.field&attributes(attributes)
		input.field__input.field__input--name(id=`${parent}-name`, type="text", name="name", placeholder="Ваше имя")
		label.visually-hidden(for=`${parent}-name`) Ваше имя
		span.field__error(aria-hidden="true") Ошибка

mixin fieldPhone(parent)
	div.field&attributes(attributes)
		input.field__input.field__input--phone(id=`${parent}-phone`, type="tel", name="phone", placeholder="Ваш телефон")
		label.visually-hidden(for=`${parent}-phone`) Ваш телефон
		span.field__error(aria-hidden="true") Ошибка

mixin fieldEmail(parent)
	div.field&attributes(attributes)
		input.field__input.field__input--email(id=`${parent}-email`, type="email", name="email", placeholder="Ваш e-mail")
		label.visually-hidden(for=`${parent}-email`) Ваш e-mail
		span.field__error(aria-hidden="true") Ошибка

mixin fieldMessage(parent)
	div.field&attributes(attributes)
		textarea.field__input.field__input--message(id=`${parent}-message`, name="message", placeholder="Сообщение")
		label.visually-hidden(for=`${parent}-message`) Ваш e-mail
		span.field__error(aria-hidden="true") Ошибка

mixin fieldConsent(parent)
	div.field.field--consent&attributes(attributes)
		input.visually-hidden(id=`${parent}-consent`, type="checkbox", name="consent", checked)
		label.field__label.field__label--consent(for=`${parent}-consent`)
			| Я согласен с обработкой личных данных #[br]
			| в соответствии с #[a(href="index.html") политикой конфиденциальности].
		span.field__error(aria-hidden="true") Ошибка

mixin fieldFile(parent, id)
	div.field.field--file&attributes(attributes)
		input.visually-hidden(id=`${parent}-file-${id}`, type="file", name="file")
		label.field__label.field__label--file(for=`${parent}-file-${id}`) Прикрепить ТЗ
