mixin social(icon)
	a.social(class=`social social--${icon}`, href="index.html")&attributes(attributes)
		span.visually-hidden Мы в #{icon}
		svg(width="20", height="20", aria-hidden="true")
			use(xlink:href=`images/sprite.svg#${icon}`)
